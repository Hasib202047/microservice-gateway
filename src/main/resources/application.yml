eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
    register-with-eureka: true
    fetch-registry: true
    refresh:
      enable: true
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 30
server:
  port: 8082


spring:
  banner:
    charset: UTF-8
    location: banner.txt
  application:
   name: @project.artifactId@
   version: @project.version@
  main:
    web-application-type: reactive
  cloud:
    gateway:
      routes:
          - id: auth_service
            uri: http://localhost:8083
            predicates:
              - Path=/user/**
            filters:
              - RewritePath=/user/(?<segment>.*), /${segment}
#          - id: order_service
#            uri: http://localhost:8082 # Order microservice
#            predicates:
#              - Path=/order/**
#          - id: product_service
#            uri: http://localhost:8083 # Product microservice

jwt:
  secret : Y2FzYW5vYmxlLXNlY3JldC1rZXktZm9yLWp3dC1oYTUxMi1hdXRoZW50aWNhdGlvbi1zeXN0ZW0tMjAyNQ==
  expiration : 3600000